<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Nur diese drei Berechtigungen. Bonfotos aus der Galerie laufen ueber den
         Photo Picker, der ohne Speicher-Permission auskommt. Meldungen werden erst
         gefragt, wenn jemand die erste Erinnerung stellt — vorher gibt es nichts
         zu melden. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Nur um die Erinnerungen nach einem Neustart wieder zu stellen: Android
         verwirft alle Wecker beim Ausschalten. Ohne das faellt eine gestellte
         Erinnerung still aus, und man merkt es erst nach der Frist. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:name=".GzgApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GzgTracker.Starting">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.GzgTracker.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Nimmt den Wecker entgegen und zeigt die Erinnerung an. Nicht
             exportiert: Nur das System selbst darf ihn ausloesen. -->
        <receiver
            android:name=".notify.ErinnerungsEmpfaenger"
            android:exported="false" />

        <!-- Stellt die Wecker nach Neustart und nach einem App-Update wieder.
             Beide Meldungen schickt nur das System, deshalb nicht exportiert. -->
        <receiver
            android:name=".notify.NeustartEmpfaenger"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
